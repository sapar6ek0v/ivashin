import React from 'react';
import {StyledFooter} from "./styles/Footer.styled.js";

const Footer = () => {
    return (
        <StyledFooter>
            FOOTER
        </StyledFooter>
    );
};

export default Footer;